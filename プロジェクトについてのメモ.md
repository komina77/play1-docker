# プロジェクトについてのメモ

## 目的

Play Framework 1系を、できるだけ長期間・再現性高く起動できる Docker イメージとして維持する。

## 採用方針

1. ベースイメージは `eclipse-temurin` 系に統一する。
2. 初期に作成した `bullseye` 系は段階的に廃止する。
3. Play バージョンごとに JDK と Python を固定し、Dockerfile 内で明示する。
4. Python は apt 依存ではなく、必要に応じてソースビルドで固定バージョンを入れる。
5. Play の小規模修正（コマンド不具合など）は、必要ならビルド時にパッチ適用する。

## バージョン別の方針（確定版）

| Play | JDK | Python | 備考 |
|---|---|---|---|
| 1.5.3 | 11 | 2.7.18 | Play 1.5.3 は Java 11 対応。スクリプトは Python2 系前提。 |
| 1.7.1 | 17 | 3.7.17 | Python3 化世代。3.8+ 以降の差分影響を避けるため 3.7 系固定で運用。 |
| 1.8.0 | 19 | 3.11 系 | 1.8.0 で Java 18/19 対応。Python 3 系不具合修正がまとまって入る。 |
| 1.9.0 | 19（主軸） | 3.11 系 | 1.8.0 の Java19 対応と関連修正を継承。17 は比較用サブタグで任意維持。 |

## 1.9.0 の扱い

1. upstream CI が 11/17 を回していても、19 非対応を意味しない。
2. 実運用は 1.9.0 も JDK19 を主軸にする。
3. 必要なら `-jdk17` タグを補助的に残し、障害切り分けや互換確認に使う。

## 運用ルール

1. README に上記マトリクスを明記する。
2. タグ命名は `<play-version>-<base>-jdk<version>` のように判別しやすくする。
3. CI では最低限 `play help` とサンプルの起動・precompile を確認する。
4. パッチを適用する場合は、対象 Play バージョンごとに `patches` を分離管理する。
5. 変更理由（JDK/Python更新・パッチ適用理由）はメモに追記して履歴を残す。
